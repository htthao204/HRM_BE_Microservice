version: '3.3'

services:
  backend:
    build: .
    ports:
      - "5000:8080"
    environment:
      PYTHONPATH: /app
      DB_HOST: ${DB_HOST}
      DB_USER: ${DB_USER}
      DB_PASS: ${DB_PASS}
      DB_NAME: ${DB_NAME}
      DB_PORT: ${DB_PORT}

